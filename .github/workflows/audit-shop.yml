name: Audit Shop residuals

on:
  push:
    paths:
      - '.github/workflows/audit-shop.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan residuals
        run: python tools/audit_shop.py
