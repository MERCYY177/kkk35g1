name: Mailbox reading layout test

on:
  push:
    branches: [ main ]
    paths:
      - 'index.html'
      - 'tools/test_mailbox_reading_layout.py'
      - '.github/workflows/mailbox-reading-layout-test.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/test_mailbox_reading_layout.py
